import {StyleSheet} from 'react-native';

export const NavigationStyle = StyleSheet.create({
  stackNavigatorStyle: {},
  iconTabNavigator: {
    resizeMode: 'contain',
    width: 30,
    height: 30,
  },
});
