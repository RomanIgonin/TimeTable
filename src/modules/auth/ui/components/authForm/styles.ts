import styled from '@emotion/native';
import { View } from 'react-native';

export const Main = styled(View)``;

export const UDButtonWrapper = styled(View)`
  align-items: center;
  margin: 15px 0 0 0;
`;
