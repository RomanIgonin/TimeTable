import {StyleSheet} from 'react-native';
import {FONT_SIZE_MIN} from '@src/core/constants';

export const LessonSwitchStyle = StyleSheet.create({
  nameLessonInputText: {
    fontSize: FONT_SIZE_MIN,
  },
});
